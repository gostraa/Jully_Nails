import styled from "styled-components";

import mobileBackground from "images/background/BG320.png";

export const MobileBackground = styled.main`
  background: url(${mobileBackground}) no-repeat center top;
`;
